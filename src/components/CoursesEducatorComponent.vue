<template>
    <div>
        <div class="content">
            <h1>Cursos</h1>
            <!--<div class="courses">
                <div class="course" v-for="course in courses" :key="course.id">
                    <h2>{{ course.name }}</h2>
                    <p>{{ course.description }}</p>
                    <p>{{ course.price }}</p>
                    <router-link :to="{ name: 'edit-course', params: { id: course.id } }">Editar</router-link>
                </div>
            </div>-->
        </div>
    </div>
</template>